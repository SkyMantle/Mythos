#!/bin/bash
set -e
if command -v systemctl >/dev/null 2>&1; then
  systemctl stop fpvscan 2>/dev/null || true
  if [ "$1" = "remove" ] || [ "$1" = "deconfigure" ]; then
    systemctl disable fpvscan 2>/dev/null || true
  fi
fi
exit 0
